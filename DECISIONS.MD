-Switched primary serving host from Oracle A1 to home-host + Cloudflare Tunnel after AD capacity failures. Tradeoff: gave up always-on public uptime for zero capacity risk and co-location with the 3080 retrain runner. Mitigated the hardware-representativeness risk by constraining the serving container to CPU-only, 2 cpus / 12g, and building multi-arch to preserve cloud portability.